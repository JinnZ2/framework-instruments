# Audit — frame instruments ordered queue, 2026-09-06

Object: the `instruments/` build of `docs/specifications/frame-instruments-ordered-queue.md`
(B1–B3), plus B4 for run-record wiring only. Read the way this ecosystem
reads a folder: test the fit, report where it breaks, repair the code to what
the specification already says, never adjust a test to stay green.

Baseline before this pass: four suites, all green, none through the run
record. Every finding below was visible only by asking which path produced a
green — the class `arena/` now records.

## Structure

```
work order ─► runrecord ─► B1 ─► B2 ─► B3        (B4 present, outside the order)
   before:      unused      ~     ~     ~         ~ = runs, violates hard constraints
   after:       wired       ✓     ✓     ✓         every entry point, every status, one code path
```

## Findings against the ordered queue (`FI_`)

| id | build | finding | disposition |
|---|---|---|---|
| FI_001 | all | `runrecord.py` existed and no script imported it; failures exited on stderr with no row. | REPAIRED. `runrecord.run()` is the one path; a body that raises becomes an `error` row. Pinned by `test_runrecord.py` and by every suite's `main()` calls. |
| FI_002 | B1, B2 | `type` field written into `summary.jsonl` and `agreement.jsonl` (`"type": "summary"`, `"type": "a_vs_d"`), against the shared rule. | REPAIRED. Summary and stability rows are told apart by structure (`jaccard`); B2 emits `agreement.jsonl` and `comparisons.jsonl`. A scan for `label/category/type/interpretation` runs over every output in every suite. |
| FI_003 | B1 | The permutation null relabelled `i` globally, so every summary was identical on the permuted file: N4 could only ever fire (arena SPEC-005). | REPAIRED. Shuffle per `(case_id, model_id, branch_rank, D)` stratum. `test_permutation_removes_cross_D_structure`: real adjacent-D Jaccard 1.0, permuted lower, per-group means unchanged. |
| FI_004 | B1 | Report section 6 printed data-integrity counters under the spec's null names (SPEC-006). | REPAIRED. N1–N5 computed from the summaries with declared thresholds printed beside each number; N5 stated NOT EVALUATED with the reason. |
| FI_005 | B1 | `report.py` exited 1 on a missing permuted summary, no `void` record. | REPAIRED. Status `void`, no report written, record carries the missing input with `sha256: null`. |
| FI_006 | B1 | Schema checked field presence only; no type checks, no cross-file rules, JSON errors carried no line number. | REPAIRED. Types, `branch_rank >= 2`, `len(continuation) <= 128`, equal lengths, unique keys, position exists in base, `forced_token` in `topk` and not `token_taken`. Seven rejection cases pinned by line and field. |
| FI_007 | B1 | `test_never_rejoin` asserted `div == 1.0` where the order asks for `div` rising with D (SPEC-012). | REPAIRED. Fixture shares a prefix and never a suffix; monotone rise asserted. |
| FI_008 | B1 | `test_summary_preserves_case_set_for_report` fed the report the same file twice (SPEC-007). | REPAIRED by the pipeline test. |
| FI_009 | B1 | `permute.py` looped forever on a single position (derangement of one). | REPAIRED. Single-position strata stay put and are counted; `path_probe` shows no test asserts that count — left standing as the arena's live finding. |
| FI_010 | B1 | Top-decile positions keyed on `i` alone, conflating cases. | REPAIRED. Positions are `[case_id, i]`. |
| FI_011 | B2 | One presentation file for four conditions; the leak check asserted key names, not content. | REPAIRED. Four files; A/D text equals the statement; B refused when the statement occurs in the key text. |
| FI_012 | B2 | `lock.py` released on reader/case match with no hash check, wrote the key to stdout, exited 1 for `void` (SPEC-008). | REPAIRED. Integrity check on release, declared output file, `void` rows, one commit per reader and case. |
| FI_013 | B2 | A vs D compared A against all D rows; the first half was not separable (SPEC-009). | REPAIRED. Conditions `D1`/`D2`; D1 rows must cite a commit hash; the A-vs-D1 check is printed first. |
| FI_014 | B2 | `order.py` used a cyclic Latin square (carry-over unbalanced) and put the shortfall on every row. | REPAIRED. Williams square blocks, shortfall in the run record notes, seed on every row. |
| FI_015 | B3 | `split.py case` emitted `{case_id, statement: null}` and no prompt; the key-role guard was a bare `assert` (SPEC-011). | REPAIRED. Prompt text per role; the case role's brief is refused if it mentions a key; validation raises `ValueError` with the line. |
| FI_016 | B3 | Drops printed to stdout only; keys rows could carry a statement. | REPAIRED. Drops counted in the run record; key rows validated to four fields. |
| FI_017 | B1 | Declared reading not stated: "rejoins the base sequence" scored as an aligned suffix match; shifted rejoins score 0. | DECLARED in `b1_score.py` and the README. Not a defect; an open reading. |

## Second work order (suite repair + arena), 2026-09-06

The package it names, `full_instrument_suite` (`gap_cases`, `frame_benchmark`,
`backcast`, `cycle_ledger`), is not in this repository, not on any branch of
either attached remote, and not under `/mnt`. The nearest in-tree referents
(`Simulators/gap-existence-cases`, `frame-location-benchmark`,
`model-deprecation-backcast`, `cycle-ledger`) do not carry the named defects:
that scorer gates on a declared `contradicted_if` list, matches on declared
keys, and verifies through `cs.verify(rec)`. Nothing was reconstructed to
stand in for it.

| item | disposition |
|---|---|
| A1–A3 | NOT IN TREE. Entered verbatim as arena seeds SPEC-001..003, unverified. |
| A4 | NOT IN TREE here. The shape (`c["id"]` KeyError on load) exists in `Simulators/frame-location-benchmark/score.py`; not modified, since that folder is outside this order's package. |
| A5 | DONE for this repository: every entry point in B1–B4 and `arena/` through `runrecord.run()`; B4 logic untouched. |
| A6 | DONE: `instruments/coverage_pairs.py` counts `(script, status)` pairs exercised over reachable, printed by `scripts/test-all.sh`, never stored. The residue at audit time is three B4 `empty` pairs that B4's own logic makes unreachable (pinned as `error`/`void` in `test_status_paths.py`). |
| PART B | BUILT: `arena/` — twelve specimens (four seeds, eight from this audit), five-state grading with the two-state refusal, `path_probe.py`, `report.py`, refutation conditions in its README. Run on this repository: one claimed branch passes disabled. |

## Cross-repository note

`Simulators` branch `claude/frame-instruments-setup-lfcntd` carries a second,
independent implementation of the same order under `frame-instruments/`,
with its own `runrecord.py`. Two builds of one order in two repositories is
the copy-drift shape this ecosystem records (`MF_019`); neither was merged
into the other here.

## Open

- `b1_permute.py` single-position stratum: reported by the probe, asserted by no test.
- Aligned-vs-shifted rejoin (FI_017) — the alternative reading is one function.
- B4 `items.py` empty input is an `error` row by its own logic; left as delivered.
- No case material for B2/B3 exists in either repository; every run above is on constructed fixtures.
